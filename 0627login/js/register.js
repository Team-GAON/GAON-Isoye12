const userInfo = {}

const login = () => {
    const id = document.getElementById('registerId');
    const password = document.getElementById('registerPassword');
    const phoneNumber = document.getElementById('registerPhoneNumber');
    userId[id] = password, phoneNumber;
};